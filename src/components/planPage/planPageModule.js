const initialState = {
	created: undefined,
	extraAmount: undefined,
	payoffOrder: []
};

export const planReducer = (state = initialState, action) => {
	switch (action.type) {

		default:
			return state;
	}
};
